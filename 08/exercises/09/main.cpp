#include "../../../std_lib_facilities.h"
#include "weight_price.h"
int main()
{
  vector<double> weight;
  vector<double> price;
  init_weight_price(weight, price);
  sum_weight_price(weight, price);
}
